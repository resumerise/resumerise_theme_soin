<div class="key-list-item" id="<%= it.id %>">
    <% if(it.label) { %>
        <label>
            <%= it.label %>
        </label>
    <% } %>
    <% if(it.data?.length) { %>
        <ul>
        <% it.data.forEach(function(item) { %>
            <li><%= item %></li>
        <% }) %>
		</ul>
    <% } %>
</div>